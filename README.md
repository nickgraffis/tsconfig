# @nickgraffis/tsconfig

```
npx @nickgraffis/tsconfig
```
* If you do not have a `package.json` it will not write one for you.
* If you already have a `tsconfig.json` - you'll be asked if you want to overwrite it.
